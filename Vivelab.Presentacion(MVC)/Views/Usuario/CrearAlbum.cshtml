@model Vivelab.Modelos.Album

@{
    ViewData["Title"] = "Crear Álbum";
}

<h1>@ViewData["Title"]</h1>

<h4>Crear un nuevo álbum</h4>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="CrearAlbum" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <!-- Nombre del Álbum -->
            <div class="form-group">
                <label asp-for="Name" class="control-label">Nombre del Álbum</label>
                <input asp-for="Name" class="form-control" required />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <!-- Fecha de Creación (predeterminada a la fecha actual) -->
            <div class="form-group">
                
                <input asp-for="CreationDate" class="form-control" type="hidden" value="@DateTime.Now.ToString("yyyy-MM-dd")" readonly />
                <span asp-validation-for="CreationDate" class="text-danger"></span>
            </div>

            <!-- Portada del Álbum -->
            <div class="form-group">
                <label asp-for="CoverUrl" class="control-label">Portada del Álbum (Imagen)</label>
                <input asp-for="CoverUrl" class="form-control" type="file" accept="image/*" />
                <span asp-validation-for="CoverUrl" class="text-danger"></span>
            </div>

            <!-- Artista Código (autoasignado) -->
            <div class="form-group">
                <input asp-for="ArtistCode" type="hidden" value="@User.FindFirst("UsuarioCodigo")?.Value" />
            </div>

            <!-- Botón de Crear -->
            <div class="form-group">
                <input type="submit" value="Crear Álbum" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver a la lista</a>
</div>
